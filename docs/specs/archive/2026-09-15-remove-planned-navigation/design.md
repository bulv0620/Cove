# 技术设计

从 `apps/web/src/app/navigation.tsx` 删除五个无路由菜单项、系统分组和不再使用的图标导入。桌面与移动端共用导航数据，因此同步生效。

删除侧栏中专门显示“规划中”禁用按钮的分支，并使导航项要求有效路由和页面元素，避免继续配置占位入口。保留已有权限过滤与路由生成逻辑。

无需数据迁移、API 或部署变更。回滚可恢复导航配置与渲染代码。完成后在产品定义中记录导航仅包含已实现页面。
