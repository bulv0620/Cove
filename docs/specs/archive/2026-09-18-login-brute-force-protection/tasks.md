# 实施任务

> Spec 已完成；实现、验证证据和长期文档已经同步。

## 阶段 0：批准条件

- [x] 维护者确认默认阈值、失败窗口、递增冷却上限和安静重置策略（需求：`AUTH-RL-FR-002`；验证：2026-09-18 会话批准及 Spec 状态）。
- [x] 审查持久化并发、HMAC 键、可信代理、审计聚合、迁移和回滚方案（需求：`AUTH-RL-FR-007` 至 `AUTH-RL-FR-011`、`AUTH-RL-NFR-001` 至 `AUTH-RL-NFR-005`；验证：2026-09-18 维护者批准且无开放问题）。
- [x] 确认 429 API/Web 契约和验收条件可执行（需求：`AUTH-RL-FR-005`、`AUTH-RL-FR-006`；验证：2026-09-18 维护者批准契约与测试计划）。

## 阶段 1：数据与配置

- [x] 新增限流状态 schema、索引和 Prisma migration，验证空库及现有库升级（需求：`AUTH-RL-FR-009`、`AUTH-RL-NFR-003`、`AUTH-RL-NFR-005`；验证：MySQL 8.0.44 空库完整迁移通过；旧 schema 中的用户、authVersion、密码凭据和审计 canary 在应用新迁移后保持一致）。
- [x] 添加有界限流参数与可信代理 CIDR 配置，更新 `.env.example`、Compose 和运维文档（需求：`AUTH-RL-FR-010`、`AUTH-RL-FR-011`；验证：默认值、合法边界、非法配置、可信/非可信代理、冲突/非法头及 Compose config 通过）。
- [x] 实现规范化与 HMAC 键派生，确保限流表不保存原始用户名/IP，派生材料不持久化或输出；审计仅按既定契约保存规范化 IP 和未知用户名截断指纹（需求：`AUTH-RL-FR-001`、`AUTH-RL-FR-009`、`AUTH-RL-NFR-004`；验证：IPv4、IPv4-mapped IPv6、IPv6 `/64`、大小写用户名及派生上下文测试通过；仓库和运行输出 canary 扫描通过）。

## 阶段 2：Server 行为

- [x] 实现 MySQL 原子双维度预检、短期校验额度预留/结算、失败转换、成功清理、递增冷却和有界清理（需求：`AUTH-RL-FR-001` 至 `AUTH-RL-FR-004`、`AUTH-RL-FR-009`；验证：状态机测试通过；两个独立 Prisma 连接下用户名 10 并发仅 5 个、同 IP 25 并发仅 20 个取得额度；重建 service 后冷却保留；过期中断预留恢复额度）。
- [x] 实现统一 429 body、`Retry-After` 与错误码，保持 401 和 dummy hash 行为（需求：`AUTH-RL-FR-005`、`AUTH-RL-NFR-001`、`AUTH-RL-NFR-002`；验证：真实 HTTP 验证前 5 次失败为 401，第 6 次为契约一致的 429，冷却后正确密码返回 201；不存在和禁用用户的 401 完全一致）。
- [x] 扩展审计结果写入并记录成功、实际失败和聚合受限事件（需求：`AUTH-RL-FR-007`、`AUTH-RL-FR-008`；验证：HTTP 验证生成 5 条 failure、1 条 rate_limited、1 条 success；重复受限使用状态计数与窗口摘要，测试后无遗留预留）。
- [x] 注入限流存储失败和并发事务失败，确认不能绕过限制或签发 token（需求：`AUTH-RL-FR-003`、`AUTH-RL-FR-011`、`AUTH-RL-NFR-003`；验证：存储预检失败时不查询用户、不签发 token；真实 MySQL 竞争测试通过）。

## 阶段 3：Web 行为

- [x] 扩展 API 错误类型并在登录页显示本地化等待提示和按绝对截止时间计算的倒计时（需求：`AUTH-RL-FR-005`、`AUTH-RL-FR-006`；验证：中英文资源、429 code/秒数解析、页面后台恢复计算、401/网络错误分支通过类型检查和生产构建）。
- [x] 保持登录表单既有响应式与主题样式，等待期间禁用提交并使用状态语义播报（需求：`AUTH-RL-FR-006`；验证：复用既有 375/768/1440px 布局、键盘表单和深浅主题组件，lint、typecheck、生产构建通过）。

## 阶段 4：安全与完成

- [x] 运行 format、lint、typecheck、build、Server 单元/集成/API 契约及 Web 构建验证（需求：全部；验证：全仓命令通过，Server 27 项通过、MySQL opt-in 项常规运行跳过并已单独通过）。
- [x] 使用隔离 canary 检查密码、JWT、原始未知用户名、请求体和派生密钥未泄露；确认完整 `keyHash` 仅存在于限流表，审计仅含独立派生的截断指纹（需求：`AUTH-RL-NFR-004`；验证：日志、响应、审计、限流表与仓库差异扫描通过；隔离 Server、数据库和容器已删除）。
- [x] 更新产品定义、身份行为、架构、部署与 NAS 运维文档，记录当前事实、配置、代理边界、迁移及回滚（需求：`AUTH-RL-FR-010`、`AUTH-RL-FR-011`、`AUTH-RL-NFR-005`；验证：文档链接、Compose 注入和事实抽查通过）。
- [x] 汇总验收证据并将 Spec 标记为 `Completed`；维护者于 2026-09-18 要求归档（需求：全部；验证：本表、长期文档和 Specs 索引同步）。

## 验证记录

| 日期       | 环境/命令                                                        | 结果                                                                                                                    | 证据                                      |
| ---------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| 2026-09-18 | `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build` | 全部通过；Web 生产构建完成                                                                                              | 本地命令输出                              |
| 2026-09-18 | `pnpm --filter @cove/server test`                                | 27 项通过；MySQL opt-in 项在常规无测试库环境按设计跳过                                                                  | Node test TAP 输出                        |
| 2026-09-18 | MySQL 8.0.44 隔离容器、完整 migrate deploy、opt-in MySQL test    | 空库 11 个迁移通过；双连接并发用户名 5/10、IP 20/25；重启状态及中断预留恢复通过                                         | `auth-rate-limit-mysql.test.cjs` TAP 输出 |
| 2026-09-18 | 旧 schema 逐迁移构造 + 用户/凭据/审计 canary + 新 migration      | `upgrade_user`、authVersion 7、密码 hash 与审计事件迁移前后相同；新增两张限流表                                         | 隔离 MySQL 查询输出                       |
| 2026-09-18 | 隔离 Server HTTP `/api/auth/login`                               | 5 次错误返回 401；第 6 次返回 429 且 header/body 秒数一致；冷却后正确密码返回 201；审计为 5 failure/1 limited/1 success | 本地 HTTP 与数据库查询输出                |
| 2026-09-18 | Prisma validate、Compose config、敏感值扫描、`git diff --check`  | schema、配置、差异和扫描通过；无测试密码/数据库 canary 残留                                                             | 本地命令输出；测试容器已删除              |
